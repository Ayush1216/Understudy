"""The deliberately hostile credit-union teller console Understudy automates against.

Two tenants of one vendor product: `alpha` (framesets, layout tables, no labels) and `beta`
(the same routes and data, reskinned). Fault injection is keyed to the session cookie.
"""
